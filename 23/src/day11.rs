use crate::{Aoc, Day11, Display, InputRep, Result};

impl Aoc for Day11 {
    fn part1(&self, _input: &InputRep) -> Result<Box<dyn Display>> {
        println!("todo!");
        result!(0)
    }

    fn part2(&self, _input: &InputRep) -> Result<Box<dyn Display>> {
        println!("todo!");
        result!(0)
    }
}
